# Public assets

Place static files such as images, videos and documents in this `public` folder.

Examples:
- `public/images/...` for hero/gallery images
- `public/videos/...` for promotional videos

You can reference these in the app using paths like `/images/example.jpg` or `/videos/intro.mp4`.

